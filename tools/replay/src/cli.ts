// Replay record/verify CLI — implemented with the sim tick API (this phase).
import { SIM_VERSION } from "@metropolis/sim";

console.error(`replay CLI: not implemented yet (sim v${SIM_VERSION})`);
process.exit(1);
