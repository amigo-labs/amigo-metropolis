// Cloudflare Worker + Durable Object lockstep relay.
// Implemented in Phase 6 (see PLAN.md); package exists so the workspace layout
// and import boundaries are fixed from day one.
export const SERVER_NOT_IMPLEMENTED = "phase-6" as const;
