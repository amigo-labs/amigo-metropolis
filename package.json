{
  "name": "amigo-metropolis",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "tools/*"
  ],
  "scripts": {
    "test": "bun test",
    "lint": "biome check .",
    "typecheck": "bun run --filter '*' typecheck",
    "build": "bun run --filter '*' build",
    "dev": "bun run --filter @metropolis/client dev",
    "gen:units": "bun run tools/gen/genUnitModels.ts",
    "replay:verify": "bun run tools/replay/src/cli.ts verify-goldens",
    "replay:verify:browser": "bun run tools/replay/src/browserVerify.ts",
    "e2e:touch": "bun run tools/replay/src/touchInput.ts",
    "verify:arenas": "bun run tools/replay/src/arenaShots.ts",
    "verify:units": "bun run tools/replay/src/unitShots.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.2",
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
